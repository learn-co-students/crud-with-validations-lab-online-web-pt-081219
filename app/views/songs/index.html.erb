<% @songs.each do |song| %>
    <tbody>
    <tr>
    <p><%= song.title %> <p>
    <p><%= song.release_year %> <p>
    <p><%= song.artist_name %> <p>
    <p><%= song.genre %> <p>
    </tr>
    </tbody>
<%end %>